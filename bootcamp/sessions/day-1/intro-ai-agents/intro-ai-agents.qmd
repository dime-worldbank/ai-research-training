---
title: "Introduction to AI Agents"
subtitle: "Day 1 · Intro to Agentic AI at the WBG"
author: "Kristoffer Bjärkefur"
institute: "Impact Analytics"
date: 5 Oct, 2026
date-format: "D MMMM YYYY"
footer: "AI-Enabled Research Bootcamp"
logos:
  - ../../../assets/images/logos/LOGO_BLACK_WB_DEC_01.png
  - ../../../assets/images/logos/AI BOOTCAMP - Branding (13).png

format:
  bootcamp-revealjs:
    fragments: true
---

```{r}
#| label: setup
#| include: false
source("../../../_extensions/bootcamp/setup_palettes.R")

knitr::opts_chunk$set(echo = FALSE, warning = FALSE, message = FALSE)
```


## Scope of This Session

:::: {.columns .v-center-container}

::: { .column width="100%" }

- Overview of agents, with an emphasis on and examples from coding agents
- Instructions for installing coding agents on WB computers
- Tools and context windows: how agents gather additional information relevant to your prompt

:::

::::

# How Does AI Work? And What Are Agents? {background-color="#530153"}

## The Two Things LLMs Can Do

No matter how amazed we are with new AI capabilities the last year, the Large Language Models (LLMs) that is the technology behind text based AI, can only do these two things. And that has not changed since the publication of ChatGPT changed the world forever.

<div style="height: 1.45em;"></div>


:::: {.columns}

::: { .column width="50%" }
### Embedding Models

- Understand meaning of text, excels at:
  - Summaries
  - Finding task in prompt
  - Identify gaps in available information
- Universal topic understanding
:::

::: {.column width="50%"}
### Generative Models

- Create text by selecting the most probable word, one word at a time
- Has generic knowledge
- Can replicate common tasks
:::

::::

## Example of the Reasoning models

:::: {.columns .v-center-container}

::: {.column width="50%" }

  ![](./img/reasoning-models.svg)

:::

::: {.column width="50%" }

### Reasoning Models

- Models review your prompt and use tools to gather additional information that "improves" your prompt
- Not a linear process - jumps back and forth as the model deems fit
- Steps are repeated until the model is satisfied

:::

::::


# Tools {background-color="#530153"}


## LLMs Cannot Do Math

### What is "3 x 7"?

::: {.fragment}
How many of you calculated it by:

- Adding 7 three times: 7 + 7 = 14, and 14 + 7 = 21
- Fetching the answer from memory
:::

::: {.fragment}

### Language models fetches from memory
LLMs can solve "3 x 7" because it appears in many multiplication tables in the training data. 

However, "35 x 22" is rarely present in multiplication tables, so LLMs do not reliably know the answer.

How can AI platforms like ChatGPT still solve 35 x 22?
:::

## But How Can AI Platforms Do Math?


AI platforms, like ChatGPT or mAI, are much more than just the model in the LLM. They have tools with instructions for when to use them.

::: {.fragment}
The model identifies the prompt is a math question. It extracts the numbers and the operator, and sends this to a calculator. The answer is then added to the prompt. 
:::
::: {.fragment}
The updated prompt becomes:
```json
{
  "human_prompt": "What is 35 x 22?",
  "calculator_tool_answer": 770
}
```
You do then not need math to generate the answer: "35 times 22 is equal to 770".
:::

::: {.fragment}
### Have you ever helped a child with math home work:

Bob, Alice, and Tom have 7 apples each. How many apples do they have together?
:::

## Agents

:::: {.columns .v-center-container}

::: {.column width="65%" }

* An agent is software that combines LLM and tools.
* It uses the LLM to review a request and decide which tools to use.
* For example, a coding agent can read code files in the project folder, adding what it finds to the prompt.
* Once the model has enough context, the agent generates a response or, for coding tasks, writes code in the project folder.

:::

::: {.column width="35%" }

  ![](./img/reasoning-models.svg)

```json
{
  "human_prompt": "What is 35 x 22?",
  "calculator_tool_answer": 770
}
```

:::

::::

## What is Context Window?

:::: {.columns .v-center-container}
::: {.column width="65%" }

* In most AI requests, much more information relevant for the task is gathered in addition to what the user provides.

* This is central to why AI performs so much better today than three years ago.

* Everything sent to the generative model is called: the context window -- the human prompt is often only a small part of it. 

* It may include full content of project files, code outputs, and web searches. Managing the context window is central to productive and cost-effective AI use.

:::
::: {.column width="35%" }

```json
{
  "human_prompt": "What is 35 x 22?",
  "calculator_tool_answer": 770
}
```

:::
::::

## What do I need to know about the Context Windows?

:::: {.columns .v-center-container}
::: {.column width="75%" }

The more information included in the context window, the better responses, right? - **NO!!**

* All context windows has a max capacity. The size of the context window depends on the model. If you surpass the limit, the AI will start making summaries of the earlier content.

* However, the closer to that limit, the less the AI can handle and keep track of details and nuances. How close you can be to the context limit depends on the specific task. 

* One character in coding can break the code, while one miscolored image pixel is not visible to the human eye.

:::
::: {.column width="25%" }

![](../img/context-window.png)

<span style="display: block; text-align: center; font-size: 0.7em; line-height: 1;">This is how we will represent context window from now on.</span>

:::
::::


# Code Agents on WB Computers {background-color="#530153"}

## Restrictions on Agents at the WB

- This field changes more quickly than ITS/OIS approves tools
- While we do not have direct access to every new tool, we have access to similar tools with similar features
- Start practicing those features now so you can adapt more quickly as more tools become available

## Which Agent is Best?

- Any definite answer will soon be outdated, so this session does not attempt one
- Anthropic has pioneered many features, and Claude is currently a market leader
- Onboarding an agent to your project's specific context may matter more than using the currently best agent
- Agent onboarding is the topic for the next two sessions and will be relevant regardless of which agent we use in the future

## Available Agents at the WB

- ITS has approved GitHub Copilot and provides licenses free to teams; it shares similar features with Claude Code
- Claude Code applications are not approved, but are being piloted
- GitHub Copilot is used through an extension in the VS Code editor and includes a light version of Claude
- The light version does not have Claude's full UI and has a smaller context window

## Set Up GitHub Copilot on WB Computers

- ITS licenses require a GitHub account. See the public best-practice guide.
- You need to be a member of a WB GitHub organization. Request membership through eService.
- Request a GitHub Copilot license through WB GitHub member access, or email `github_enterprise_admin@worldbankgroup.org`.
- Request a VS Code installation through eService.
- The remaining steps will be demonstrated now.

## Demo: Brief Introduction to VS Code

Demo project: <https://github.com/dime-worldbank/ce-coding-agent-demo>

## Demo: Set Up GitHub Copilot

## Demo: Use Claude through GitHub Copilot

## Demo: Manage Spending

# Context Window {background-color="#530153"}

## Context Window

- The context window holds the information and details that generative models use to generate an answer one word at a time
- Only what is in the context window is available to the LLM; anything relevant that is missing will be guessed from the model's generic knowledge
- Including project-specific details in the context window is how a coding agent can produce quality output even when correct answers are not frequently available on the internet

## Tools

- Tools add content to the context window
- If the context window only contains the human prompt "What is 35 x 22?", the generative model may guess a number
- If the AI system has a calculator tool, the calculation is sent to the calculator and the answer is inserted into the context window
- The generative model can then generate natural language that answers the math question

### Calculator tool result

`35 x 22 = 770`

## Agentic Loop and Context Window

:::: {.columns}

::: {.column width="55%"}
1. Prompt
2. Review prompt
3. Use tools
4. Draft an answer
5. Review completeness
6. Answer or action

The agent repeats the review, tool-use, drafting, and completeness steps as needed.
:::

::: {.column width="45%"}
### Context window contents

- System prompt
- Human prompt: "What is 35 x 22?"
- Calculator tool result: `35 x 22 = 770`
- Additional context added by the agent
:::

::::

## Coding Agent Tools

For coding agents, the main tool is searching project files and inserting the relevant parts into the context window.

### Which prompt gets the most correct and helpful answer?

**A.** Paste only the error code and ask for a solution.

**B.** Paste the error code and relevant parts of project files that could be related to the error.

Option B is a central best practice in prompt engineering. Coding agents do this best practice for you.

## Sessions

- Each chat or agent execution is called a session. Each session has its own context window.
- Each prompt and answer is added to the context window, and the full conversation is submitted for each new prompt.
- Edit and resubmit your previous prompt rather than asking the AI to "forget" previous prompts or answers.

## Manage Your Context Window

- All models have a maximum context window. If the maximum length is reached, the model will summarize earlier parts.
- The more content in the context window, the more details the model must account for in its attention.
- Reset the context window by starting a new chat or session when changing topic.

## Planning Mode

- Use planning mode to build a context window for a more complex task.
- In planning mode, the agent asks for human input only when it identifies missing relevant context.
- The process repeats until you decide the remaining gaps can be filled by the model's generic knowledge. Then ask it to execute.

## Demo: Planning Mode

# Next Sessions {background-color="#530153"}

## Next Sessions: Onboarding

- Agents are good at building the context window for you, but as a domain expert, you are even better
- The next two sessions cover memory files and skills, which can provide context for all or specific prompts
- Memory and skills make agents faster and reduce token consumption
- They also reduce the risk that agents guess from generic knowledge that does not apply to our specific context

## Next Sessions: Sign Up

- **Memory files** - 12:30 PM to 2:00 PM EDT, June 25 - sign up through the session registration link
- **Skills** - 9:30 AM to 11:00 AM EDT, July 7 - sign up through the session registration link

# Thank You! {background-color="#530153"}

## Questions?

## Reasoning Models

:::: {.columns}

::: {.column width="60%"}
1. Prompt
2. Review prompt for missing context
3. Use tools to fetch context and update prompt
4. Generate a draft answer
5. Review answer completeness
6. Answer
:::

::: {.column width="40%"}
- Models review and improve input and its own output
- Not a linear process
- Steps are repeated until the LLM is satisfied
:::

::::

## What is an Agent?

:::: {.columns}

::: {.column width="60%"}
1. Prompt
2. Review prompt
3. Use tools
4. Draft an answer
5. Review completeness
6. Answer or Action
:::

::: {.column width="40%"}
- An agent is a system known as the agentic loop, which heavily depends on LLMs
- While the LLM determines the next task to undertake, it is the agent that carries out the execution of that task
- The LLMs typically run on servers in the cloud. Those servers do not have access to files on your computer. But the agent can have that
:::

::::

## Where is the Agent Middleware Installed?

Agents can be installed anywhere — but where it is installed matters to what information it can access.

:::: {.columns}

::: {.column width="33%"}
### Internet-based services

- Such as: chatgpt.com, claude.ai/code
- Access:
  - Publicly available information
  - Content you’ve connected it to
  - Not WB owned resources such as GitHub
- Any project-specific context must be pasted manually into your prompt
:::

::: {.column width="33%"}
### Enterprise intranet

- Such as: mAI on WB intranet
- Access:
  - Publicly available information
  - WB content ITS have connected it to
- Most project-specific context must be pasted manually into your prompt
:::

::: {.column width="33%"}
### On your computer

- Such as: GitHub Copilot or Claude Code in VS Code, or Claude Desktop
- Access:
  - Publicly available information
  - Project files on your computer
  - Any project-specific context not in your files must be pasted manually into your prompt
:::

::::